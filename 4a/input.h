#ifndef INPUT_H
#define INPUT_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int inp_int(int *num);
int check_command();
int myln(const char *st);
void summator(char *res, char *sm, size_t n);
char *in_str();


#endif
